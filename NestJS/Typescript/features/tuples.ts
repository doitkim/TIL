type Drink = [string, boolean, number];
type carStats = {
  horsepower: number;
  weight: number;
};

const pepsi: [string, boolean, number] = ["brown", true, 40];
const sprite: Drink = ["clear", true, 40];
const tea: Drink = ["brown", false, 0];
const carSpecs: carStats = {
  horsepower: 400,
  weight: 3354,
};
